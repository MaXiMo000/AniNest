# Copy this to .env and fill in real values. Never commit .env itself.

PORT=8787
NODE_ENV=development

# Origin(s) allowed to call this API with credentials. Comma-separated for multiple.
FRONTEND_ORIGIN=http://localhost:5173

# Used to derive session token hashing pepper. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Rotating this instantly invalidates every existing session.
SESSION_SECRET=change-me-to-a-long-random-value

# Path to the SQLite file. Leave unset for local dev (defaults to
# backend/data/aninest.db). On a host with an ephemeral filesystem, point
# this at a mounted persistent disk instead — see DEPLOY.md.
# DB_PATH=/var/data/aninest.db

# Optional: Cloudflare Turnstile secret key, for bot-checking registrations.
# Free at https://dash.cloudflare.com/?to=/:account/turnstile — leave unset
# to skip bot verification entirely (fine for local dev / personal use).
# TURNSTILE_SECRET_KEY=
