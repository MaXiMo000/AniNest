// Slugs shared between the leaderboard/score routes and (informally) the
// frontend's route paths - kept as a single source of truth so validating a
// `:game` route param and adding a future game both happen in one place.
export const GAMES = ['higher-lower', 'guess-the-anime'];
