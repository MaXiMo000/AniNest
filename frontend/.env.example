VITE_API_URL=http://localhost:8787

# Optional: Cloudflare Turnstile site key (public, safe to expose client-side).
# Leave unset to skip the bot-check widget on registration entirely.
# VITE_TURNSTILE_SITE_KEY=
